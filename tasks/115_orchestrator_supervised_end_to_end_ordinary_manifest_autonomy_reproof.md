# Task 115 — Orchestrator supervised end-to-end ordinary-manifest autonomy re-proof

## Why this task exists

After the autonomy operating-mode hardening tasks land, the orchestrator should re-prove itself over a short ordinary manifest that uses the stronger multi-role and carry-forward surfaces together.

## Outcome

Add a supervised local-first end-to-end re-proof over a short ordinary manifest using ordinary-task execution surfaces, tester critique/replay, repair memory, and cross-task carry-forward.

## Create or update these exact files

- `tests/test_task_queue.py`
- `tests/test_multi_project_adapters.py`
- `tests/test_project_bootstrap_adapter.py`
- `tests/test_run_task_runtime_foundations.py`
- `tests/test_failure_journal.py`
- `docs/TRADINGBOT_PROJECT_STATE.md`
- `docs/ORCHESTRATOR_PRODUCT_SPEC.md`
- `README.md`

## Required behavior

The re-proof should demonstrate only a bounded supervised slice, including at most:

1. short ordinary-manifest progression across more than one ordinary task
2. truthful combined use of coder/tester/controller surfaces together
3. tester critique and focused replay before broader validation where relevant
4. repair-memory suppression of repeated no-progress attempts
5. bounded cross-task carry-forward memory
6. conservative stop behavior when authority or admission gates are unsatisfied
7. no broader claim than the deterministic proof actually covers

## Acceptance

This task is complete when the repo has a fresh supervised local-first end-to-end ordinary-manifest re-proof after Tasks 108–114, with docs synchronized narrowly and honestly.
